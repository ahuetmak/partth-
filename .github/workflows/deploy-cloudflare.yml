# Deploy a Cloudflare Pages (GitHub → Cloudflare Direct Upload)
# Evita fallas de conexión: GitHub empuja a Cloudflare en lugar de que Cloudflare tire
name: Deploy to Cloudflare Pages

on:
  push:
    branches: [main, cursor/cursor-verificaci-n-ventas-partth-fb17, copilot/deploy-ultima-actualizacion-sistema]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.API_CLOUDFLARE }}
          accountId: ${{ secrets.ID_CUENTA_CLOUDFLARE }}
          projectName: partth
          directory: files
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}

      - name: Purge Cloudflare cache (partth.com)
        continue-on-error: true
        run: |
          if [ -n "${{ secrets.CLOUDFLARE_ZONE_ID }}" ]; then
            curl -s -X POST "https://api.cloudflare.com/client/v4/zones/${{ secrets.CLOUDFLARE_ZONE_ID }}/purge_cache" \
              -H "Authorization: Bearer ${{ secrets.API_CLOUDFLARE }}" \
              -H "Content-Type: application/json" \
              --data '{"hosts":["partth.com"]}'
          fi
